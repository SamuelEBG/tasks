package pgr112.step7;

import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;

import java.awt.Color;
import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertTrue;

/*
public class CircleTest {

    @Test
    public void testCircle1(){
        Circle c1 = new Circle(1 );

        Assertions.assertEquals(1, c1.getId());
        Assertions.assertFalse(c1.isFilled()); // test if the default value is false in the asbtract class constructor for shapes
        Assertions.assertEquals(Color.RED, c1.getColor()); // test if the color is default set to red in the abstract class constructor for shapes
        Assertions.assertEquals(1.0, c1.getRadius()); // test the default radius in the abstract class constructor for shapes

        Assertions.assertEquals(2*Math.PI, c1.getPerimeter());
        Assertions.assertEquals(Math.PI, c1.getArea());
    }

    @Test
    public void testCircle2(){
        Circle c2 = new Circle(5, Color.red, true, 5); //set a new circle with perimeter values

        Assertions.assertEquals(5, c2.getId());
        Assertions.assertEquals(Color.RED, c2.getColor()); // test if the color actually sets to red by calling getColor
        Assertions.assertEquals(Math.PI*25.0, c2.getArea()); // test if the Area is PI * r^2
        Assertions.assertEquals(Math.PI*10.0, c2.getPerimeter()); // test if the perimeter is PI * r+r
    }
}
*/