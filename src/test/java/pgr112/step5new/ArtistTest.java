package pgr112.step5new;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

import java.util.ArrayList;

import static org.junit.jupiter.api.Assertions.*;

class ArtistTest {
 /*
    private Program prog;
    private ArrayList<Artist> testArray;

    @BeforeEach
    void init(){
        prog = new Program();
        testArray = new ArrayList<>(prog.artists);
    }

    @Test
    void testArray(){

        int size = testArray.size();

        testArray.remove(0);

        assertEquals(size, testArray.size()-1);
    }

  */

}